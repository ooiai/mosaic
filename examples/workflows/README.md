# Workflow Examples

- `research-brief.yaml`: minimal runnable workflow example using the mock provider

Run it:

```bash
mosaic run examples/workflows/research-brief.yaml --workflow research_brief --session workflow-demo
```
