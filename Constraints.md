Always preserve existing functionality, API compatibility, and prior business behavior; make only the smallest necessary changes to fix bugs, improve performance, and complete requirements, avoid modifying stable logic unless truly required, extract duplicated code into reusable methods, utilities, or components, and ensure all changes pass regression validation without breaking existing behavior.
